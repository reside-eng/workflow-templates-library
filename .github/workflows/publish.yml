name: Publish

on:
  push:
    branches:
      - main
      - next
      - alpha
      - beta
      # N.x (maintenance release branches)
      - '[0-9]+.x'

concurrency:
  group: publish-${{ github.ref }}

env:
  NODE_VERSION: 24.x

jobs:
  publish:
    name: publish
    timeout-minutes: 25
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Use Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          # TODO: Once we have dependencies, enable this for caching (requires yarn.lock)
          # cache: yarn

      - name: Configure package manager
        run: |
          echo Configuring NPM_TOKEN globally for .npmrc
          npm config set '//registry.npmjs.org/:_authToken' ${{ env.NPM_TOKEN }}
          npm set @reside-eng:registry "https://npm.pkg.github.com"
          npm config set '//npm.pkg.github.com/:_authToken' $GITHUB_TOKEN
          npm whoami
        env:
          NPM_TOKEN: ${{ secrets.NPM_READ_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Semantic Release
        uses: cycjimmy/semantic-release-action@b12c8f6015dc215fe37bc154d4ad456dd3833c90 # v6.0.0
        id: semantic
        with:
          semantic_version: ^24
          branches: |
            [
              '+([0-9])?(.{+([0-9]),x}).x',
              'main',
              'next',
              'next-major',
              {name: 'beta', prerelease: true},
              {name: 'alpha', prerelease: true}
            ]
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # The v* aliases are protected by the release-alias-branches org ruleset, whose bypass actor
      # is the SIDE_CI App (GitHub refuses the implicit GitHub Actions app as an org-ruleset bypass
      # actor). The alias push must therefore authenticate as SIDE_CI, not GITHUB_TOKEN.
      # Recursion-safe: no workflow here triggers on push to v* branches.
      - name: Get SIDE_CI token for the alias push
        id: alias-token
        if: steps.semantic.outputs.new_release_published == 'true'
        uses: peter-murray/workflow-application-token-action@cb731e00cb6754f584507134ff389dfb3dc42bbc # v5.1.0
        with:
          application_id: ${{ secrets.SIDE_CI_APPLICATION_ID }}
          application_private_key: ${{ secrets.SIDE_CI_APPLICATION_PRIVATE_KEY }}

      - name: Push updates to branch for major version
        # If a new version is published, i.e. v1.2.3 then this step will update
        # branch "v1" to this commit.
        # https://github.com/reside-eng/workflow-templates-library/branches
        if: steps.semantic.outputs.new_release_published == 'true'
        env:
          # App installation token, NOT the built-in Actions token - named accordingly
          ALIAS_PUSH_TOKEN: ${{ steps.alias-token.outputs.token }}
        run: |
          git push https://x-access-token:${ALIAS_PUSH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git HEAD:refs/heads/v${{steps.semantic.outputs.new_release_major_version}}

  notification:
    if: always()
    name: notification
    needs: [publish]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - uses: technote-space/workflow-conclusion-action@45ce8e0eb155657ab8ccf346ade734257fd196a5 # v3.0.3

      - uses: reside-eng/workflow-status-notification-action@v1
        with:
          current-status: ${{ env.WORKFLOW_CONCLUSION }}
          slack-webhook: ${{ secrets.SLACK_WEBHOOK_PLATFORM_PROD }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
